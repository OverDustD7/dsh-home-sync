# dsh-home-sync 0.2.0 修复完成报告

> 后续更正：本报告记录第一轮修改，不代表第 4 节维护建议全部完成或所有边界已覆盖。二次审查发现 6 项遗漏，并在 0.2.1 继续修复和补齐维护建议。当前状态以 [二次审查与修改报告](D:/Project/DSH/dsh-home-sync/DSH插件二次审查报告.md) 为准。

日期：2026-09-07。依据用户“直接修改”的授权，已修改当前项目代码，版本由 0.1.0 更新为 0.2.0。

**原报告中的 17 项问题均已落实代码修复。** 本次没有重启真实 DSH、修改真实主目录配置、访问真实远端或提交用户数据。原审计报告和原始复现结果作为历史证据保留。

## 实际修改

| 文件 | 修改内容 |
| --- | --- |
| [lib/index.js](D:/Project/DSH/dsh-home-sync/lib/index.js) | 宿主鉴权与来源校验、严格 JSON 请求解析、定时器动态更新、统一错误响应和完整路由注销 |
| [lib/sync.js](D:/Project/DSH/dsh-home-sync/lib/sync.js) | 新增独立同步核心：配置校验、同步白名单、跨进程锁、分支检查、合并与推送、预检备份及初始化回滚 |
| [lib/ui.js](D:/Project/DSH/dsh-home-sync/lib/ui.js) | 修复图标点击、键盘和备用入口；显示真实状态、持久面板日志；防重复提交；修正初始化说明和界面清理 |
| [package.json](D:/Project/DSH/dsh-home-sync/package.json) | 版本更新为 0.2.0，声明 Node >=20，增加 check/test 命令，无新增第三方依赖 |
| [README.md](D:/Project/DSH/dsh-home-sync/README.md) | 安装、生效、白名单、配置、初始化备份恢复、锁与验证说明 |
| [test](D:/Project/DSH/dsh-home-sync/test) | 后端、事件模型、真实浏览器和已安装宿主兼容性测试 |
| [audit](D:/Project/DSH/dsh-home-sync/audit) | 保留历史证据，旧复现命令转为执行当前正确性测试，增加当前测试结果与浏览器截图 |

## 对照原报告的修复结果

| 问题 | 已实现的行为 | 主要验证 |
| --- | --- | --- |
| F01 接口鉴权缺失 | 注入 connection，每个数据接口在处理前调用宿主 requestRejection；服务缺失时不注册接口；POST 只接受 JSON | 无身份 401、不可信 Host/Origin 403、错误内容类型 415；真实本地 HTTP 测试 |
| F02 初始化覆盖禁止文件 | 在独立临时仓库获取目标树，拒绝禁止路径、链接、子模块与本机路径碰撞；不对 DSH_HOME 执行 hard reset | 远端虚构凭据、符号链接、本机硬链接均被拒绝，本地内容保持原样 |
| F03 备份不完整 | 使用原 HEAD、索引、远端目标文件的并集备份；同时保存原 .git、配置、清单及校验值；备份位于主目录之外 | .gitignore、mnemon/extra.json、本地暂存内容及 Git 历史均有恢复材料 |
| F04 全量暂存 | 只暂存允许路径，检查完整索引、目标树及全部待推送提交的树 | 未跟踪凭据不上传；已暂存凭据、历史中曾提交的禁止文件会阻止推送 |
| F05 重试误报成功 | 是否需要 commit 与是否需要 push 分开；干净工作区也重试未上传的提交 | 首次推送失败、恢复远端后不改文件即可上传，ahead 归零 |
| F06 双端修改停滞 | 先 fetch；网络失败不提交；不同文件改动可合并；内容冲突停止发布并撤销此次未完成合并，保留本地提交 | 双端不同文件收敛；同文件冲突保留本机内容且远端不变 |
| F07 开关不生效 | 保存设置后取消旧计时器并按新设置创建；每次执行前读取当前开关 | 开启、关闭、间隔变更立即生效，已取消回调也不能继续推送 |
| F08 分支不一致 | 普通同步要求实际分支匹配设置；初始化受控切换目标分支，设置 origin/upstream 并保存分支 | experiment 不能误推 main；使用非 main 初始化后，分支与配置一致 |
| F09 并发无互斥 | 所有写操作共用主目录旁的文件系统锁；同进程和不同服务实例互斥；忙碌时明确拒绝 | 同时操作、运行期间改配置返回 busy；完成后锁释放 |
| F10 卸载残留 | 收集并注销全部路由、HTML 注入与计时器；卸载阻止后续 Git 步骤 | 卸载后 6 条路由清零；重载无冲突；进行中的操作停止后释放锁 |
| F11 配置不校验 | 统一 schema、分支和远端验证，限制类型/长度/间隔；损坏配置暂停自动任务；临时文件落盘后替换 | 非法配置拒绝；旧字段按明确优先级迁移；损坏配置不会启用默认任务 |
| F12 请求体处理错误 | 按字节限额收集 Buffer，再严格解码 UTF-8；拒绝无效 JSON、非对象、超时和中断请求 | 中文和 emoji 在每个字节分割点均完整；异常输入明确 4xx |
| F13 初始化失败不恢复 | 更改主目录前完成远端预检；操作失败尝试恢复文件、索引、HEAD、远端和配置 | fetch 失败 origin 不变；模拟最终配置写入失败后完整恢复原状态 |
| F14 SVG 点击误关闭 | 使用包含关系判定外部点击，以 button click 激活，拖拽后抑制点击 | SVG/path 点击稳定打开，外部点击关闭，拖拽不误开 |
| F15 键盘入口失效 | 支持原生 button click，补充可访问名称、展开状态、焦点进入/返回及焦点样式 | 键盘生成的 click 打开，Escape 关闭 |
| F16 备用入口无效 | 弹窗使用实际触发元素定位，备用入口不再依赖主按钮 | 模拟主按钮创建前失败，备用按钮仍能打开面板 |
| F17 失败显示零变化 | 返回仓库/配置错误，界面显示“未知”和失败详情；增加领先/落后和最近操作状态 | 无效仓库、损坏配置不再报告成功或 0 待提交 |

附加修复：Windows 长短路径采用实际路径比较；状态读取关闭 Git 可选锁，减少与写操作争用；初始化拒绝依赖外部共享对象库的仓库；临时目录清理失败不掩盖已完成的初始化结果；弹窗快速重开不产生重复 id；页面脚本重新挂载会清理旧的全局监听器及计时器。

## 验证结果

最终回归测试：**35 项通过，0 失败，0 跳过**。

- 28 项后端/适配器测试：真实 Git 临时仓库与本地远端，包含失败、冲突、非法路径、数据保护、回滚、互斥、卸载、开关及请求体场景。
- 5 项界面事件测试：点击、键盘激活、拖拽抑制、备用入口、外部点击与清理。
- 1 项真实 Chrome 无头测试：使用全新临时用户目录和虚构 API 响应，验证 SVG 点击、中文设置保存、Escape 关闭和快速重开。
- 1 项宿主兼容性测试：复用本机 DSH 的 WebServer 路由方法和 HostConnectionService 来源校验，通过真实本地 HTTP 请求验证拒绝、授权调用、卸载与重载。

宿主兼容性测试的 Cookie 身份验证使用虚构实现，**不代表已经验证真实登录 Cookie、完整 Cordis 启动流程或真实远端链路**。没有进行真实账号 SSH/HTTPS 推送、Linux/macOS、断电恢复或其他插件并发写入的端到端测试。

验证环境：Windows、Node 24.20.0、Git 2.55.0.windows.4、本机 DSH 0.1.2-rc.1、Chrome。浏览器因沙箱无法启动图形子进程，最终在正常用户权限下运行；仍使用独立临时配置，仅访问测试 HTML。

证据：[完整回归结果 XML](D:/Project/DSH/dsh-home-sync/audit/regression-results.xml)、[浏览器结果](D:/Project/DSH/dsh-home-sync/audit/browser-regression-result.json)、[浏览器截图](D:/Project/DSH/dsh-home-sync/audit/browser-regression.png)。

## 生效与操作变化

当前插件已通过本地 `link:` 接入 DSH；**重启 DSH web，再刷新页面**即可读取修改后的后端和界面。本次未自动重启，以免中断正在运行的 DSH 工作。

以下变化需要知悉：

1. 操作正在运行时，新的写操作及设置保存返回忙碌；完成后可重试。卸载允许当前 Git 子进程结束，但不再开始后续步骤。
2. 初始化成功后自动任务暂停。先检查返回的备份路径，按需要手动合并内容，再重新开启自动同步。
3. 冲突不会被自动覆盖；普通同步遇到冲突会保留本地提交并停止推送，需要手动解决。
4. 两种初始化模式都备份。备份位于 `<DSH_HOME>.home-sync-backups/init-*`，配置与锁说明见 README。
5. 意外断电/强制结束仍可能留下锁或需要手动恢复。先停止 DSH，按备份 manifest 和 MERGE.md 恢复，再处理遗留锁。
6. 插件锁只能协调本插件，无法冻结 DSH 其他插件或人工 Git 操作。初始化期间应避免其他写入；目前拒绝 worktree、链接 .git 和外部共享对象库的初始化。
7. 禁止路径检查不等于允许文件内部的敏感字段识别。跨设备的本地 link 路径仍需各设备自行安装源码。

本次没有新增外部依赖、发布包、提交 Git、推送真实数据或修改真实凭据。
