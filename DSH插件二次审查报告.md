# dsh-home-sync 0.2.1 二次审查与修改报告

日期：2026-09-07。范围：当前 dsh-home-sync 项目。依据用户“都修改了”的指示，补齐原报告第 4 节维护建议，并修复二次审查确认的 6 项遗漏。

## 结论与更正

原“4. 其他限制与维护建议（不计入 17 项缺陷）”此前只完成了一部分；现在已按该节建议落实代码、文档和版本化安装包。第一轮“17 项均修复”及后续“未发现新的高风险缺陷”表述不能代表全部边界正确，本次发现并修复的 6 项问题如下。历史报告保留，并添加更正入口。

## 原第 4 节逐项对照

| 原建议 | 当前实现 | 状态 |
| --- | --- | --- |
| 远端单独更新需要周期检查 | 自动同步即使本地干净也 fetch、整合远端，失败后可重试；已有远端单独更新回归通过 | 已完成，保留前轮实现 |
| 跨设备安装不应依赖开发机固定路径 | 提供 0.2.1 tgz 包，README 给出复制包后用 DSH 安装的步骤、初始化后重装及解压 link 备用方案；无需复用 D:/Project/DSH 路径 | 已完成，新增安装包 |
| 状态刷新与可查操作反馈 | 每 15 秒刷新；新目录按文件统计；默认折叠“操作记录”，可选中复制备份路径及恢复指引；后台/手动操作持久化，重启后仍可查询 | 已完成 |
| 主题与生命周期 | 面板、输入框、按钮、记录区和浮动入口使用主题变量；宿主/系统主题变化原地更新，保留表单；保留统一监听器、定时器和观察器清理 | 已完成 |
| 后台 Git 行为 | 保留非交互凭据模式、超时、退出码判断及 Git 环境变量隔离；新增禁用自动暂存、合并后索引检查、禁用替换对象影响历史检查 | 已完成 |
| 工程维护 | README、Node/宿主兼容性与恢复说明、check/test 命令已具备；0.2.1 运行包含 7 个必要文件（含中英文 README）；补齐复审测试和历史结论更正 | 已完成 |

操作即时反馈继续使用底部 toast，隐藏的 aria-live 区仍保留。新的“操作记录”只有用户展开时显示，不会在保存设置后自动展开日志框。

## 二次审查的 6 项修复

| 编号/等级 | 原行为与影响 | 修复与验证 |
| --- | --- | --- |
| R01 / P1 | 拉取地址和推送地址不同，历史检查仅排除拉取仓库已有提交；旧提交中的禁止文件会被上传到新推送仓库。即使 HEAD 已删除文件仍可泄漏历史内容 | [sync.js](D:/Project/DSH/dsh-home-sync/lib/sync.js) 检查 HEAD 完整可达历史，校验所有 push URL，限制检查量，不自动跟随标签或镜像引用。虚构秘密历史在多目标推送前被拒绝；干净历史可推送多个目标 |
| R02 / P1 | 用户开启 merge.autoStash 时，拉取快进后恢复暂存冲突可返回 Git 退出码 0，插件误报成功，文件实际出现 UU 和冲突标记 | 显式 --no-autostash，并在整合后检查索引。冲突拉取失败，原 HEAD、本地未提交内容保持不变，无新增 stash 或未合并索引 |
| R03 / P2 | 已 git init、尚未首次提交的有效空仓库被当作无效 HEAD，设备初始化失败 | 初始化区分 unborn 与损坏引用；空历史按空树处理，仍检查并备份索引文件。测试确认原暂存文件及索引备份可读取 |
| R04 / P2 | Git 把新目录折叠成 mnemon/，状态过滤后显示 0 待提交，但实际同步会上传内部文件 | status 使用 --untracked-files=all。新增两份记忆分别计数，排除目录继续排除，允许文件能正常推送 |
| R05 / P2 | 后端初始化关闭自动同步和启动拉取，打开面板仍显示勾选；再保存其他设置可能重新启用自动任务 | 初始化响应返回实际配置，[ui.js](D:/Project/DSH/dsh-home-sync/lib/ui.js) 立即刷新表单。真实浏览器验证两开关清零，随后保存不会重新开启 |
| R06 / P2 | 第二次初始化到无关根历史时，固定内部 init 引用触发 non-fast-forward 拒绝，虽然回滚成功，但初始化未完成 | 从已验证临时仓库按目标对象抓取，不更新旧固定 init 引用。独立根历史切换成功，完整备份仍保留 |

R06 最初在共享祖先的场景未复现；后续使用独立根提交确认失败，故正式纳入修复。不会把初次未复现误当作最终排除结论。

## 操作记录与安装交付

记录文件位于 `<DSH_HOME>.home-sync-history.json`，在同步仓库外。最多保存最近 100 条并限制总字节数；使用临时文件、fsync 和替换写入。内容为时间、操作类型、结果消息及初始化备份路径，不保存同步文件正文。损坏记录保留并提示，写记录失败不会撤销已成功的主操作。该文件不是初始化备份本身，完整恢复材料仍在 `.home-sync-backups` 下。

[版本化安装包](D:/Project/DSH/dsh-home-sync/dist/dsh-home-sync-0.2.1.tgz) 包含英文 README.md、中文 README.zh-CN.md、package.json、cordis.patch.yml 和 lib 下 3 个模块。未公开发布到包仓库。安装命令与迁移顺序见 [README](D:/Project/DSH/dsh-home-sync/README.md)。复制包到新机器后，需在那里安装；同步 profile 声明不会自动传输包文件。

## 验证与证据

- 原有 35 项回归在修改前全部通过，仍被独立探针发现遗漏，说明旧覆盖并不充分。
- 本次新增 7 项后端测试和 1 项真实浏览器测试；完整回归 **43 通过、0 失败、0 跳过**。结果：[maintenance-tests.xml](D:/Project/DSH/dsh-home-sync/audit/maintenance-tests.xml)。
- 后续记录警告和主题取色微调，对相关场景再次验证；真实浏览器额外关闭测试动画，检查记录区没有被折叠容器裁切，并检查截图。[浏览器结果](D:/Project/DSH/dsh-home-sync/audit/maintenance-browser-result.json)、[截图](D:/Project/DSH/dsh-home-sync/audit/maintenance-browser.png)。
- 最终版本增加中文长记录的总字节上限验收，连同重复初始化、持久化和界面相关场景定向复跑 **8 通过、0 失败、0 跳过**。[最终定向结果](D:/Project/DSH/dsh-home-sync/audit/maintenance-final-targeted-tests.xml)。
- `npm run check` 检查 3 个运行模块语法。安装包在独立临时目录解压，核对全部条目及逐文件字节一致性，并成功导入运行模块。[包验证与 SHA-256](D:/Project/DSH/dsh-home-sync/audit/package-verification.json)。
- 修改前缺陷证据保留于 [后端探针结果](D:/Project/DSH/dsh-home-sync/audit/second-review-results.json) 和 [界面探针结果](D:/Project/DSH/dsh-home-sync/audit/second-review-browser-results.json)。这些结果记录旧行为，当前正确性验收使用 test 下的回归测试。

所有 Git 场景均使用临时目录、虚构文件与本地测试远端。浏览器使用独立无头配置及模拟接口；宿主测试复用已安装宿主实现。未运行真实账号的跨设备联网测试，也未在真实 DSH 主目录安装、初始化、推送或重启服务。

## 生效与边界

当前本地 link 安装读取项目代码，后端变更需重启 DSH web，界面需刷新。其他机器使用本次安装包自行安装。

历史检查采取保守策略：最多 1000 个可达提交；即使禁止文件只存在于旧历史也会停止发布。多推送仓库不具备跨仓库原子事务，网络中断后可能需要重试。Git hooks/签名仍遵循用户配置；插件锁不能阻止外部程序写文件；强制结束进程后的恢复仍应按备份说明执行。这些限制已写入维护文档，并未通过强推或删除历史规避。
